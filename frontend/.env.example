# Backend base URL. Keep `/api` when using Vite proxy in local dev.
VITE_API_BASE_URL=/api

# Optional: direct browser header injection (use only for controlled local/dev environments).
# VITE_API_KEY=replace-with-api-key

# Optional demo mode.
VITE_DEMO_MODE=false

# Optional request timeout in milliseconds.
VITE_API_TIMEOUT_MS=15000

# Optional Vite proxy settings.
BACKEND_TARGET=http://127.0.0.1:8080
# BACKEND_API_KEY=replace-with-api-key
